import 'package:flutter/material.dart';

class fingerprint_auth extends StatelessWidget {
  const fingerprint_auth({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
